
public class Student {
	String name;

	public String getName() {
		return name;
	}

	public void setName(String name) {
		if (name== null || name.trim().name.equals(""))
		{
			throw new IllegalArgumentException("Name is mandatory");
		}
		this.name = name;
	}
	
}
